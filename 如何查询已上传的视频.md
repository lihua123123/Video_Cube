# 📚 如何查询已上传的视频

## 🎯 方法一:使用视频库界面(推荐)

### 在前端页面查看

1. **启动后端服务器**(如果还没启动)
   ```bash
   cd Backend
   npm start
   ```

2. **启动前端开发服务器**(如果还没启动)
   ```bash
   cd Frontend
   npm run dev
   ```

3. **打开浏览器访问前端页面**

4. **点击"📚 视频库"按钮**
   - 在页面底部的操作按钮栏中
   - 紫色的"📚 视频库"按钮

5. **在视频库中你可以:**
   - ✅ 查看所有已上传的视频(带缩略图)
   - ✅ 搜索视频(按标题搜索)
   - ✅ 查看视频详情(时长、创建时间等)
   - ✅ 点击播放按钮直接播放视频
   - ✅ 删除不需要的视频
   - ✅ 分页浏览(每页12个视频)

### 视频库功能说明

**顶部控制栏:**
- 🔍 **搜索框**: 输入关键词搜索视频标题
- 🔄 **刷新按钮**: 重新加载视频列表

**视频卡片显示:**
- **缩略图**: 视频的预览图
- **标题**: 视频名称
- **描述**: 视频描述(如果有)
- **时长**: 视频播放时长
- **创建时间**: 上传时间(如"今天"、"昨天"、"3天前"等)
- **状态**: 正常/处理中

**操作按钮:**
- ▶️ **播放**: 点击后在主页面播放该视频
- ℹ️ **详情**: 查看视频的完整信息
- 🗑️ **删除**: 删除该视频(需确认)

**底部分页:**
- ← **上一页** / **下一页** →
- 显示当前页码和总页数

---

## 🎯 方法二:使用后端API查询

### 1. 使用浏览器直接访问

打开浏览器访问:
```
http://localhost:3000/admin/videos
```

这会返回JSON格式的视频列表。

### 2. 使用命令行查询

**方法A: 使用 curl**
```bash
curl http://localhost:3000/admin/videos
```

**方法B: 使用 PowerShell**
```powershell
Invoke-RestMethod -Uri "http://localhost:3000/admin/videos" | ConvertTo-Json -Depth 10
```

**带参数的查询:**
```bash
# 限制数量
curl "http://localhost:3000/admin/videos?limit=5"

# 带偏移量(分页)
curl "http://localhost:3000/admin/videos?limit=10&offset=10"

# 搜索关键词
curl "http://localhost:3000/admin/videos?keyword=测试"
```

### 3. 使用 Node.js 脚本查询

我已经为你创建了一个查询脚本 `Backend/check_videos.js`:

```bash
cd Backend
node check_videos.js
```

这个脚本会显示:
- 数据库中的视频总数
- 每个视频的详细信息(ID、标题、URL等)

**你也可以测试 API:**
```bash
cd Backend
node test_api.js
```

这会测试视频列表 API 并显示前5个视频。

---

## 🎯 方法三:直接查询数据库

### 使用 MySQL 客户端

1. **连接到数据库**
   ```bash
   mysql -u root -p
   # 输入密码: 123456
   ```

2. **选择数据库**
   ```sql
   USE video_cube;
   ```

3. **查询视频列表**
   ```sql
   -- 查询所有视频
   SELECT * FROM videos ORDER BY created_at DESC;
   
   -- 查询最新10个视频
   SELECT id, title, duration, status, created_at 
   FROM videos 
   ORDER BY created_at DESC 
   LIMIT 10;
   
   -- 查询特定标题的视频
   SELECT * FROM videos WHERE title LIKE '%Desktop%';
   
   -- 统计视频数量
   SELECT COUNT(*) as total FROM videos;
   
   -- 按状态分组统计
   SELECT status, COUNT(*) as count FROM videos GROUP BY status;
   ```

---

## 📊 当前数据库状态

根据最近的查询,你的数据库中已经有:

- ✅ **至少 10 个视频记录** (ID: 23-32)
- ✅ 主要是 "Desktop 2025.11.22 - 10.57.14.02" 这个视频
- ✅ 所有视频都有缩略图
- ✅ 状态都是 `active`(正常)
- ✅ 时长范围: 15秒 - 557秒

**视频文件位置:**
- 视频文件: `Backend/uploads/videos/`
- 缩略图: `Backend/uploads/thumbnails/`

---

## 🔧 常见问题

### Q1: 为什么我之前看不到上传的视频?

**A:** 这是因为数据库表结构不完整。现在已经修复了,添加了缺失的4个字段:
- `file_name`
- `thumbnail_name`
- `file_size`
- `resolution`

现在所有功能都正常了!

### Q2: 如何删除重复的视频?

**A:** 可以使用以下方法:

**方法1: 通过前端视频库**
- 打开视频库
- 点击视频卡片上的 🗑️ 删除按钮
- 确认删除

**方法2: 通过API**
```bash
curl -X DELETE http://localhost:3000/admin/videos/{视频ID}
```

**方法3: 通过数据库**
```sql
DELETE FROM videos WHERE id = 视频ID;
```

### Q3: 如何清理所有视频?

**⚠️ 警告: 这会删除所有视频数据!**

```sql
-- 清空视频表
TRUNCATE TABLE videos;

-- 重置自增ID
ALTER TABLE videos AUTO_INCREMENT = 1;
```

然后手动删除上传的文件:
```bash
cd Backend
# Windows PowerShell
Remove-Item -Path "uploads\videos\*" -Force
Remove-Item -Path "uploads\thumbnails\*" -Force
```

### Q4: 如何备份视频数据?

```bash
# 导出数据库
mysqldump -u root -p video_cube videos > videos_backup.sql

# 备份文件
xcopy /E /I uploads uploads_backup
```

---

## 📱 API 接口文档

### 获取视频列表
```
GET /admin/videos
参数:
  - limit: 每页数量(默认10)
  - offset: 偏移量(默认0)
  - keyword: 搜索关键词

返回:
{
  "status": true,
  "message": "查询视频列表成功",
  "data": {
    "videos": [
      {
        "id": 1,
        "title": "视频标题",
        "description": "视频描述",
        "video_url": "/uploads/videos/xxx.mp4",
        "thumbnail_url": "/uploads/thumbnails/xxx.jpg",
        "duration": 125,
        "status": "active",
        "created_at": "2025-11-23...",
        "updated_at": "2025-11-23..."
      }
    ],
    "total": 10
  }
}
```

### 获取单个视频详情
```
GET /admin/videos/:id

返回:
{
  "status": true,
  "message": "查询视频详情成功",
  "data": {
    "video": { ... }
  }
}
```

### 删除视频
```
DELETE /admin/videos/:id

返回:
{
  "status": true,
  "message": "删除视频成功"
}
```

---

## 🎉 总结

现在你有**4种方法**可以查询已上传的视频:

1. ✅ **视频库界面** - 最直观、最方便(推荐)
2. ✅ **浏览器访问API** - 适合快速查看
3. ✅ **命令行脚本** - 适合调试和批量操作
4. ✅ **数据库查询** - 适合数据分析和维护

所有数据都在数据库中,功能完全正常! 🎊
