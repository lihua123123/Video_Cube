// 前端调试指南 - 视频库显示问题

## 问题诊断步骤

### 1. 打开浏览器开发者工具
- 按 F12 或右键 -> 检查
- 切换到 Console(控制台) 标签页

### 2. 刷新页面,查看控制台输出
应该看到视频数据的日志输出

### 3. 检查 Network(网络) 标签页
- 刷新页面
- 查找 `/api/admin/videos` 请求
- 点击查看:
  - Status: 应该是 200
  - Response: 查看返回的JSON数据
  - Preview: 查看格式化的数据

### 4. 检查图片加载
- 在 Network 标签页中筛选 Img
- 查找缩略图请求:
  - `/api/uploads/thumbnails/xxx_thumbnail.jpg`
  - 应该返回 200 状态码
  - 如果是 404,说明缩略图路径有问题

### 5. 检查DOM元素
- 切换到 Elements 标签页
- 找到 `.video-item` 元素
- 检查:
  - `.video-thumbnail img` 的 src 属性
  - `.video-title` 是否有文字内容
  - `.video-meta-row` 是否显示时间

### 6. 常见问题排查

#### 如果看不到任何视频:
- 控制台是否有错误?
- Network中API请求是否成功?
- videos 数组是否为空?

#### 如果看到灰色占位图标,没有缩略图:
- 检查缩略图URL是否正确
- 检查Network中图片请求是否404
- 检查 `getFullUrl()` 函数是否正确添加 `/api` 前缀

#### 如果没有标题/时间:
- 检查 `video.title` 是否有值
- 检查 `video.created_at` 是否有值
- 检查 `formatDate()` 和 `formatDuration()` 函数

### 7. 临时添加调试代码

在 VideoLibrary.vue 的 fetchVideos 函数中添加:

```javascript
const fetchVideos = async () => {
  // ... 现有代码 ...
  
  if (response.data.status) {
    videos.value = response.data.data.videos || [];
    totalCount.value = response.data.data.total || videos.value.length;
    
    // 添加这些调试日志
    console.log('📹 视频列表加载成功!');
    console.log('总数:', totalCount.value);
    console.log('当前页视频数:', videos.value.length);
    if (videos.value.length > 0) {
      console.log('第一个视频:', videos.value[0]);
      console.log('- 标题:', videos.value[0].title);
      console.log('- 缩略图:', videos.value[0].thumbnail_url);
      console.log('- 时长:', videos.value[0].duration, '秒');
      console.log('- 创建时间:', videos.value[0].created_at);
    }
  }
}
```

### 8. 检查CSS样式

在浏览器开发者工具中:
- 选中一个视频卡片
- 查看 Styles 标签页
- 确认样式是否正确应用

### 9. 测试URL

在浏览器地址栏直接访问:
- `http://localhost:5173/api/admin/videos?limit=8&offset=0`
  - 应该看到JSON数据
- `http://localhost:5173/api/uploads/thumbnails/afc212d1-c205-465c-a780-24afe6bd44fd_thumbnail.jpg`
  - 应该看到缩略图图片

如果以上测试失败,说明代理配置有问题。
